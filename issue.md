# issue.md

## 확인된 이슈

1. `.env.sample`이 삭제되어 환경 변수 예시를 바로 확인하기 어려운 상태였습니다.
   - 영향: 신규 참여자나 배포 환경에서 필요한 키 이름을 놓치기 쉽습니다.
   - 대응: `README.md`와 실제 서버 코드 기준으로 예시 파일을 다시 맞추는 편이 좋습니다.

2. `Motipe_bot/server.js`가 단독 실행과 마운트 실행을 동시에 고려하지 못할 수 있었습니다.
   - 영향: 다른 Express 앱에 연결할 때 서버가 중복으로 `listen()`될 가능성이 있습니다.
   - 대응: `require.main === module` 조건으로 단독 실행만 listen하도록 정리했습니다.

3. 프론트엔드에서 API 호출 경로가 절대 경로로 고정되어 있었습니다.
   - 영향: `/bot` 같은 서브 경로로 서비스할 때 경로 충돌 가능성이 있습니다.
   - 대응: `api/...` 형태의 상대 경로로 바꿔 현재 마운트 구조에 맞췄습니다.

4. 초기 안내 문구가 실제 입력 흐름과 맞지 않았습니다.
   - 영향: 사용자가 `[행사]` 또는 `[축제]` 키워드를 입력해야 한다고 오해할 수 있습니다.
   - 대응: `[지역명]` 입력 안내로 수정했습니다.

5. README 내용이 현재 프로젝트 구조와 맞지 않게 축약되어 있었습니다.
   - 영향: 프로젝트 소개, 실행 방법, 기능 설명을 찾기 어렵습니다.
   - 대응: 핵심 기능과 실행 방법만 남기고 구조를 단순화했습니다.

## 남은 주의점

- 루트와 `Motipe_bot/`에 병행 구조가 남아 있어, 실제 배포 진입점이 어디인지 계속 맞춰봐야 합니다.
- `.DS_Store` 같은 OS 생성 파일은 커밋 대상에서 제외하는 편이 좋습니다.
- `Motipe_bot/docs/ISSUE_축제검색_UI개선.md`는 인코딩이 깨져 보여 별도 정리가 필요할 수 있습니다.
