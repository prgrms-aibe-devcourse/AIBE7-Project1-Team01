# issue.md

## 변경 중 확인된 이슈

### 1. 환경 변수 예시 파일이 삭제되어 있었음
- 확인 위치: `.env.sample`
- 문제 내용: 기존에 있던 환경 변수 예시 파일이 삭제되어 있어, 어떤 키를 준비해야 하는지 한눈에 확인하기 어려웠습니다.
- 영향:
  - 신규 참여자가 로컬 실행에 필요한 값을 빠르게 맞추기 어렵습니다.
  - Render나 다른 배포 환경에 변수 등록 시 누락 가능성이 커집니다.
- 현재 조치:
  - 저장소 루트에 `.env.example`을 두는 방향으로 정리했습니다.
- 추가 권장사항:
  - `README.md`와 실제 서버 코드에 나오는 환경 변수 목록을 항상 동일하게 유지하는 편이 좋습니다.

### 2. 서버 진입점이 단독 실행과 모듈 마운트를 동시에 고려하지 못했음
- 확인 위치: `Motipe_bot/server.js`
- 문제 내용: `server.js`가 바로 `listen()`을 호출하는 구조였기 때문에, 다른 Express 앱에서 `require()`로 불러서 마운트할 때 중복 실행 위험이 있었습니다.
- 영향:
  - 루트 서버와 `Motipe_bot` 서버를 함께 쓸 때 포트 충돌이 날 수 있습니다.
  - 개발 환경에서 의도치 않게 서버가 두 번 떠 있는 것처럼 동작할 수 있습니다.
- 현재 조치:
  - `require.main === module` 조건으로 단독 실행일 때만 `listen()`하도록 수정했습니다.
  - `module.exports = app` 형태로 외부 마운트를 허용했습니다.
- 추가 권장사항:
  - 병행 구조가 계속 유지된다면, 어떤 파일이 실제 운영 진입점인지 문서로 명확히 적어두는 것이 좋습니다.

### 3. 프론트엔드 API 경로가 절대 경로로 고정되어 있었음
- 확인 위치: `Motipe_bot/public/app.js`
- 문제 내용: `fetch('/api/...')`처럼 절대 경로를 쓰고 있어, 서브 경로에 올렸을 때 라우팅이 꼬일 여지가 있었습니다.
- 영향:
  - `/bot` 같은 경로에 앱을 얹는 경우 API 호출이 잘못된 위치로 갈 수 있습니다.
  - 환경에 따라 동일 코드가 다르게 동작할 수 있습니다.
- 현재 조치:
  - `api/...` 형태의 상대 경로로 바꿔 현재 마운트 구조를 따르게 했습니다.
- 추가 권장사항:
  - 배포 경로가 바뀔 가능성이 있으면 프론트에서 상대 경로를 우선하는 편이 안전합니다.

### 4. 초기 안내 문구와 실제 입력 규칙이 맞지 않았음
- 확인 위치: `Motipe_bot/public/index.html`
- 문제 내용: 초기 안내가 `[행사]` 또는 `[축제]` 같은 키워드를 입력하라고 되어 있었는데, 실제 흐름은 지역명 기반 입력에 더 가깝습니다.
- 영향:
  - 사용자가 무엇을 입력해야 하는지 혼동할 수 있습니다.
  - 초반 이탈 가능성이 올라갑니다.
- 현재 조치:
  - `[지역명]` 입력 안내로 바꿔 실제 사용 흐름과 맞췄습니다.
- 추가 권장사항:
  - 시작 문구는 기능 설명보다 “지금 무엇을 입력해야 하는지”를 먼저 알려주는 쪽이 좋습니다.

### 5. README가 현재 프로젝트 구조를 충분히 설명하지 못했음
- 확인 위치: `README.md`
- 문제 내용: 문서가 길게 정리돼 있던 반면, 실제 현재 구조와 맞는 핵심 정보가 잘 드러나지 않았습니다.
- 영향:
  - 실행 방법과 기능 범위를 빠르게 파악하기 어렵습니다.
  - 협업자가 프로젝트 현황을 읽기 위해 추가 탐색을 해야 합니다.
- 현재 조치:
  - 프로젝트 소개, 기술 스택, 핵심 기능, 실행 방법 위주로 압축 정리했습니다.
- 추가 권장사항:
  - 실제 구현이 바뀌면 README도 같이 갱신해서 “문서와 코드가 따로 노는 상태”를 줄이는 것이 좋습니다.

### 6. 루트와 `Motipe_bot/`에 병행 구조가 남아 있음
- 확인 위치: 프로젝트 루트 전체 구조
- 문제 내용: 루트 앱과 `Motipe_bot` 앱이 함께 존재해서, 어떤 파일이 최신 기준인지 헷갈릴 수 있습니다.
- 영향:
  - 진입점과 정적 파일 위치를 잘못 수정할 가능성이 있습니다.
  - 배포 설정이 이중화될 수 있습니다.
- 현재 조치:
  - 이번 변경에서는 실제 사용되는 쪽을 기준으로 정리했지만, 구조 자체는 아직 병행 상태입니다.
- 추가 권장사항:
  - 다음 작업에서는 루트 앱과 하위 앱 중 하나를 기준 구조로 확정하는 편이 좋습니다.

### 7. OS 생성 파일과 작업 흔적이 커밋 대상에 섞일 수 있었음
- 확인 위치: `.DS_Store`, `.claude/`
- 문제 내용: macOS 계열의 `.DS_Store`나 도구 작업 폴더가 워크트리에 섞여 있었습니다.
- 영향:
  - 저장소가 불필요한 파일로 더러워집니다.
  - 리뷰 시 실제 코드 변경과 무관한 잡음이 늘어납니다.
- 현재 조치:
  - `.gitignore`에 `.DS_Store`를 추가했습니다.
- 추가 권장사항:
  - 도구 폴더는 커밋 전 항상 `git status`로 한 번 더 확인하는 것이 좋습니다.

## 메모
- `Motipe_bot/docs/ISSUE_축제검색_UI개선.md`는 인코딩이 깨져 보였기 때문에, 필요하면 별도로 UTF-8 기준으로 다시 정리하는 것이 좋습니다.
- 현재 문서는 “발견된 이슈” 중심이며, 코드 수정 내역 전체를 설명하는 문서는 아닙니다.
